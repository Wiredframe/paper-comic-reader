## 2026-07-27
- Gebaut: Reject cover dates whose day doesn't exist in the month → PR #1
- Verworfen (für später): Unit tests for `ReaderPaging` (ComicReader/Reader/ReaderCollectionController.swift:21-44); unit tests for `ComicInfoParser.stories(fromSummary:)` (ComicReader/Model/ComicInfo.swift:131-182); unit tests for `Bookmark.resolvedStoryIndex`/`assignStory`/`clearStory` (ComicReader/Model/Bookmark.swift:96-128); unit tests for `ComicBook.sortsBefore` (ComicReader/Model/ComicBook.swift:209-221); `Importer.prepare` bare-extension title fallback for pathological file names (ComicReader/Model/Importer.swift:52,71).
- Notiz: No Xcode/Swift toolchain in this sandbox (Linux, no `swift`/`xcodebuild`) — verification is by careful reading only, no `xcodebuild test` run. Repo's normal habit is committing straight to main (see release.yml comment), but this session's assigned workflow uses a dedicated branch + PR instead. Good next picks: the `ReaderPaging` test gap (pure struct, zero UIKit dependency, doc comment already claims edge cases are "verified" but nothing proves it) or `ComicInfoParser.stories(fromSummary:)` tests (trickiest regex logic in the model layer, zero coverage).
